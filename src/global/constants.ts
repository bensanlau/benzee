import { DieItem } from "../components/die/die";

export const NUMBER_OF_ROLLS: number = 3;
export const NUMBER_OF_DICE: number = 5;
export const UNROLLED_DIE: DieItem = {
  value: null,
  locked: false,
}
